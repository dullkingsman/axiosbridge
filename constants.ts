/**
 * Time before giving up on server
 * discovery.
 */
export const DEFAULT_CONNECTION_TIME_OUT = 10000;
/**
 * Time before giving up on response form a
 * server after a handshake.
 */
export const DEFAULT_REQUEST_TIME_OUT = 5000;
